{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MeetingSummary",
  "type": "object",
  "required": ["objectives", "synthesis", "key_issues", "conclusion_next_steps", "business_perspective", "verbatims"],
  "additionalProperties": false,
  "properties": {
    "objectives": {
      "type": "array",
      "description": "Objectifs principaux de la réunion",
      "items": { "type": "string" }
    },
    "synthesis": {
      "type": "array",
      "description": "Synthèse des échanges — analyse libre, style homogène : **Thème en gras** — explication concise. Sous-bullets possibles via '::' et ';'.",
      "items": { "type": "string" }
    },
    "key_issues": {
      "type": "array",
      "description": "Principaux enjeux — risques, opportunités, arbitrages. Style homogène.",
      "items": { "type": "string" }
    },
    "conclusion_next_steps": {
      "type": "array",
      "description": "Conclusion et prochaines étapes — recommandations stratégiques et actions. Style homogène.",
      "items": { "type": "string" }
    },
    "business_perspective": {
      "type": "string",
      "description": "Mise en perspective business pour Elements, nourrie par le corpus interne (Strategy / Operations / Digital / Impact)."
    },
    "verbatims": {
      "type": "array",
      "description": "Citations clés professionnelles (pas personnelles).",
      "items": {
        "type": "object",
        "required": ["quote", "speaker"],
        "additionalProperties": false,
        "properties": {
          "quote": { "type": "string", "maxLength": 240 },
          "speaker": { "type": "string", "description": "Nom, rôle, ou 'Note' si inconnu" }
        }
      }
    }
  }
}